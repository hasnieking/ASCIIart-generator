//general.h
#ifndef GENERAL_H
#define GENERAL_H
#include <string>

const int COLOURSIZE = 24;  //bits per pixel

std::string hexendianconverter(const std::string input);
int hextoint(const std::string input);

#endif